"use client";
import {useMemo,useState} from "react";
import {useActionState} from "react";
import {saveAttendance} from "@/app/dashboard/step-5-actions";
import type {ActionState} from "@/app/dashboard/school-admin/setup/actions";
type Student={id:string;first_name:string;last_name:string;admission_number:string}; type Entry={student_id:string;status:string|null;remarks:string|null};
const initial:ActionState={ok:false,success:false,message:""};
export function AttendanceMarker({students,initialEntries,context,readOnly=false}:{students:Student[];initialEntries:Entry[];context:{academic_session_id:string;term_id:string;class_id:string;arm_id:string;attendance_date:string};readOnly?:boolean}){const seed=Object.fromEntries(students.map(s=>{const e=initialEntries.find(x=>x.student_id===s.id);return[s.id,{status:e?.status??null,remarks:e?.remarks??""}]}));const [values,setValues]=useState<Record<string,{status:string|null;remarks:string}>>(seed),[query,setQuery]=useState("");const [state,action,pending]=useActionState(saveAttendance,initial);const visible=useMemo(()=>students.filter(s=>`${s.first_name} ${s.last_name} ${s.admission_number}`.toLowerCase().includes(query.toLowerCase())),[students,query]);const counts=students.reduce((a,s)=>{a[values[s.id]?.status??'unmarked']++;return a},{present:0,absent:0,late:0,excused:0,unmarked:0} as Record<string,number>);const entries=students.filter(s=>values[s.id]?.status).map(s=>({student_id:s.id,status:values[s.id].status,remarks:values[s.id].remarks}));return <form action={action} className="space-y-5"><input type="hidden" name="entries" value={JSON.stringify(entries)}/>{Object.entries(context).map(([k,v])=><input key={k} type="hidden" name={k} value={v}/>)}<div className="grid grid-cols-2 gap-2 sm:grid-cols-5" aria-live="polite">{Object.entries(counts).map(([k,v])=><div className="rounded-xl border p-3" key={k}><b>{v}</b><p className="text-xs capitalize text-muted-foreground">{k}</p></div>)}</div>{!readOnly?<button type="button" className="min-h-11 rounded-xl border px-4 font-semibold" onClick={()=>setValues(Object.fromEntries(students.map(s=>[s.id,{...values[s.id],status:'present'}])))}>Mark all present</button>:null}<label className="block"><span className="sr-only">Search students</span><input className="h-12 w-full rounded-xl border px-4" placeholder="Search student or admission number" value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="space-y-3">{visible.map(s=><article className="rounded-xl border p-4" key={s.id}><div><b>{s.last_name} {s.first_name}</b><p className="text-sm text-muted-foreground">{s.admission_number}</p></div><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{['present','absent','late','excused'].map(status=><button disabled={readOnly} type="button" aria-pressed={values[s.id]?.status===status} className={`min-h-11 rounded-xl border text-sm font-semibold capitalize ${values[s.id]?.status===status?'bg-primary text-white':''}`} key={status} onClick={()=>setValues({...values,[s.id]:{...values[s.id],status}})}>{status}</button>)}</div>{values[s.id]?.status==='excused'?<input disabled={readOnly} required className="mt-3 h-11 w-full rounded-xl border px-3" placeholder="Excuse reason required" value={values[s.id].remarks} onChange={e=>setValues({...values,[s.id]:{...values[s.id],remarks:e.target.value}})}/>:<details className="mt-3"><summary className="min-h-11 cursor-pointer py-3 text-sm font-semibold">Remarks</summary><input disabled={readOnly} className="h-11 w-full rounded-xl border px-3" value={values[s.id]?.remarks??''} onChange={e=>setValues({...values,[s.id]:{...values[s.id],remarks:e.target.value}})}/></details>}</article>)}</div>{state.message?<p className={state.ok?'text-primary':'text-red-600'}>{state.message}</p>:null}{!readOnly?<div className="sticky bottom-0 flex gap-2 border-t bg-white/95 py-3"><button disabled={pending} name="intent" value="draft" className="min-h-11 flex-1 rounded-xl border font-semibold">Save draft</button><button disabled={pending||counts.unmarked>0} name="intent" value="submit" className="min-h-11 flex-1 rounded-xl bg-primary text-white font-semibold">Submit register</button></div>:null}</form>}

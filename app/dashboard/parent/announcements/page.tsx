import {PageHeader} from "@/components/dashboard/page-header";import {AnnouncementFeed} from "@/components/announcements/announcement-feed";import {getSchoolContext} from "@/lib/school-context";export default async function Page(){const c=await getSchoolContext();const [{data:rows},{data:reads}]=await Promise.all([c.supabase.from('announcements').select('id,title,body,priority,publish_at,published_at,is_pinned').eq('school_id',c.schoolId).order('publish_at',{ascending:false}).limit(50),c.supabase.from('announcement_reads').select('announcement_id').eq('school_id',c.schoolId).eq('user_profile_id',c.profileId)]);return <div className="space-y-6"><PageHeader eyebrow="Parent portal" title="Announcements" description="Current in-app notices for you and your linked children."/><AnnouncementFeed rows={rows??[]} readIds={(reads??[]).map(x=>x.announcement_id)}/></div>}
